#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main(){

    float x = 3;

    //Squareroot, Powers, Round, Round Up Ceil, Round Down Floor, Absolute value (negatives), log, sincostan, 

    //x = sqrt(x);
    //x = pow(x, 2);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x = abs(x);
    //x = log(x);
    //x = sin(x);
    //x = cod(x);
    //x = tan(x);

    printf("%f", x);

    return 0;
}